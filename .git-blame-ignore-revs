# Commits that are pure formatting. `git blame` skips them so it keeps pointing
# at whoever actually wrote a line, not at the reformat.
#
# Enable locally (one time):
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub honours this file automatically in its blame view.

# chore(format): apply Prettier across the source tree
43e549ee7927c1c421c86e06f32a004e8a188056
